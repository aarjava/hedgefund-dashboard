## 2024-05-22 - Static Analysis for UX
**Learning:** UX improvements like tooltips and typo fixes can be verified using AST parsing when UI testing is unavailable.
**Action:** Use `ast` module to verify presence of specific arguments (like `help`) in UI component calls.
