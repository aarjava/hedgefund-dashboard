## 2024-05-24 - Streamlit Accessibility
**Learning:** Streamlit components like `st.slider` and `st.metric` support a `help` parameter that renders as a tooltip. This is a low-effort way to add context to quantitative inputs without cluttering the UI.
**Action:** Always check for `help` parameters in Streamlit input/display widgets to improve explainability of financial metrics.
